{ ... }:

{
  system.defaults.CustomUserPreferences.NSGlobalDomain = {
    # Mouse > Advanced > "Pointer acceleration" off: pointer travel scales linearly
    # with hand movement, regardless of speed.
    "com.apple.mouse.linear" = true;
  };
}
