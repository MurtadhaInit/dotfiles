[init]
	defaultBranch = main
[user]
	name = Murtadha
	email = 78985176+MurtadhaInit@users.noreply.github.com

[core]
	editor = code --wait
	autocrlf = input
[diff]
	tool = vscode
[difftool "vscode"]
	cmd = "code --wait --diff $LOCAL $REMOTE"
[merge]
	tool = vscode
[mergetool "vscode"]
    cmd = code --wait $MERGED

[alias]
    lg = log --oneline --decorate --graph --all --pretty
    unstage = restore --staged .

# Delta theme setup
[include]
	path = ./delta/catppuccin.gitconfig
[delta]
	features = catppuccin-mocha